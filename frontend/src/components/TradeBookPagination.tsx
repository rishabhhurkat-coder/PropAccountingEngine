export function TradeBookPagination({ page, pageCount, totalCount, pageSize, onPageChange, onPageSizeChange }: { page: number; pageCount: number; totalCount: number; pageSize: number; onPageChange: (page: number) => void; onPageSizeChange: (pageSize: number) => void }) {
  const pages = pageCount >= 6 ? [1, 2, 3, -1, pageCount] : Array.from({ length: pageCount }, (_, index) => index + 1);
  return <div className="reference-pagination"><span>Showing {totalCount ? (page - 1) * pageSize + 1 : 0} to {Math.min(page * pageSize, totalCount)} of {totalCount} results</span><div className="reference-page-buttons"><button disabled={page === 1} onClick={() => onPageChange(page - 1)}>‹</button>{pages.map((item, index) => item === -1 ? <span key={`ellipsis-${index}`}>...</span> : <button key={item} className={page === item ? 'active' : ''} onClick={() => onPageChange(item)}>{item}</button>)}<button disabled={page === pageCount} onClick={() => onPageChange(page + 1)}>›</button><select aria-label="Rows per page" value={pageSize} onChange={(event) => onPageSizeChange(Number(event.target.value))}><option value="10">10 / page</option><option value="15">15 / page</option><option value="25">25 / page</option><option value="50">50 / page</option><option value="100">100 / page</option></select></div></div>;
}
