import { Bar, BarChart, Cell, LabelList, ResponsiveContainer, XAxis, YAxis } from 'recharts';
import { useReportData } from './report-data';
import { ReportCard } from './report-card';
export function ProfitFactorDistribution() { const { profitFactorDist } = useReportData(); return <ReportCard title="Profit Factor Distribution" subtitle="# Strategies"><div className="v0-distribution-chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={profitFactorDist} margin={{ top: 14, right: 6, left: -20, bottom: 0 }}><XAxis dataKey="bucket" tickLine={false} axisLine={false} fontSize={8} tickMargin={5} /><YAxis tickLine={false} axisLine={false} fontSize={8} allowDecimals={false} domain={[0,3]} ticks={[0,1,2,3]} /><Bar dataKey="count" radius={[3,3,0,0]} barSize={40}><LabelList dataKey="count" position="top" fontSize={9} fill="#6b7596" />{profitFactorDist.map((item) => <Cell key={item.bucket} fill={item.color} />)}</Bar></BarChart></ResponsiveContainer></div></ReportCard>; }
export function MaxDrawdownDistribution() { return <ReportCard title="Max Drawdown Distribution (₹)"><div className="v0-chart-caption">Available after strategy-level drawdown expansion.</div></ReportCard>; }
