import { Bar, CartesianGrid, ComposedChart, Line, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { ChevronDown } from 'lucide-react';
import { useReportData } from './report-data';
import { ReportCard } from './report-card';
export function PnlTrendChart() { const { pnlTrend } = useReportData(); const data = pnlTrend.map((item) => ({ ...item, month: new Date(`${item.date}T00:00:00`).toLocaleDateString('en-IN', { day: '2-digit', month: 'short' }) })); return <ReportCard title="P&L Trend (All Strategies)" action={<button className="v0-select">Cumulative <ChevronDown size={13} /></button>}><div className="v0-trend-chart"><ResponsiveContainer width="100%" height="100%"><ComposedChart data={data} margin={{ left: 4, right: 7, top: 8, bottom: 0 }}><CartesianGrid vertical={false} stroke="#e9edf4" strokeDasharray="3 3" /><XAxis dataKey="month" tickLine={false} axisLine={false} tickMargin={7} fontSize={9} interval={Math.max(0, Math.floor(data.length / 10))} /><YAxis tickLine={false} axisLine={false} tickFormatter={(v) => Math.abs(v) >= 100000 ? `₹${(v / 100000).toFixed(0)}L` : v === 0 ? '₹0' : `₹${(v / 1000).toFixed(0)}K`} fontSize={9} width={39} /><Tooltip /><Bar dataKey="pnl" name="Daily P&L" fill="#2dbd83" radius={[2,2,0,0]} barSize={12} /><Line type="monotone" dataKey="cumulative" name="Cumulative P&L" stroke="#2458ff" strokeWidth={2.2} dot={false} /></ComposedChart></ResponsiveContainer></div><div className="v0-chart-legend"><span><i className="blue-dot" />Cumulative P&L (₹)</span><span><i className="green-dot" />Daily P&L (₹)</span></div></ReportCard>; }
