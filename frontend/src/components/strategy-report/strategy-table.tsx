import { formatINR, useReportData } from './report-data';
import { ReportCard } from './report-card';
const headers = ['Strategy','Total P&L (₹)','Total Trades','Win Rate (%)','Profit Factor','Avg Win (₹)','Avg Loss (₹)','Max Drawdown (₹)'];
export function StrategyTable() { const { strategyRows } = useReportData(); return <ReportCard title="Strategy Performance Summary"><div className="v0-table-content"><div className="v0-table-scroll"><table className="v0-strategy-table"><thead><tr>{headers.map((header,index)=><th key={`${header}-${index}`} className={index===0?'left':''}>{header}</th>)}</tr></thead><tbody>{strategyRows.map((row,index)=><tr key={`${row.name}-${index}`}><td className="left"><span className="v0-strategy-name"><i style={{background:row.pnl >= 0 ? '#2dbd83' : '#ef4444'}} />{row.name}</span></td><td className={row.pnl >= 0?'v0-positive':'v0-negative'}>{formatINR(row.pnl)}</td><td>{row.trades.toLocaleString('en-IN')}</td><td>{row.winRate.toFixed(2)}%</td><td>{row.profitFactor.toFixed(2)}</td><td>{formatINR(row.avgWin)}</td><td className="v0-negative">{formatINR(row.avgLoss)}</td><td className="v0-negative">{formatINR(row.maxDrawdown)}</td></tr>)}</tbody></table></div></div></ReportCard>; }
