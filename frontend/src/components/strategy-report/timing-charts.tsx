import { Area, AreaChart, Bar, BarChart, CartesianGrid, Cell, ReferenceLine, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import { useReportData } from './report-data';
import { ReportCard } from './report-card';
const axis=(v:number)=>v===0?'₹0':Math.abs(v)>=100000?`₹${(v/100000).toFixed(1)}L`:`₹${(v/1000).toFixed(0)}K`;
export function DayOfWeekChart() { const { dayOfWeek } = useReportData(); return <ReportCard title="Day of Week Performance"><div className="v0-timing-chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={dayOfWeek} margin={{top:12,right:7,left:-8,bottom:0}}><CartesianGrid vertical={false} stroke="#e9edf4"/><XAxis dataKey="day" tickLine={false} axisLine={false} fontSize={9}/><YAxis tickLine={false} axisLine={false} fontSize={9} tickFormatter={axis} width={40}/><ReferenceLine y={0} stroke="#dfe6f0"/><Bar dataKey="value" radius={[3,3,0,0]} barSize={28}>{dayOfWeek.map(item=><Cell key={item.day} fill={item.value>=0?'#2dbd83':'#ef4444'}/>)}</Bar><Tooltip/></BarChart></ResponsiveContainer></div></ReportCard>; }
export function TimeOfDayChart() { const { timeOfDay } = useReportData(); return <ReportCard title="Time of Day Performance" subtitle="(Intraday)"><div className="v0-timing-chart"><ResponsiveContainer width="100%" height="100%"><AreaChart data={timeOfDay} margin={{top:12,right:8,left:-8,bottom:0}}><defs><linearGradient id="strategy-report-tod" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#2458ff" stopOpacity={.18}/><stop offset="100%" stopColor="#2458ff" stopOpacity={0}/></linearGradient></defs><CartesianGrid vertical={false} stroke="#e9edf4"/><XAxis dataKey="time" tickLine={false} axisLine={false} fontSize={8} interval={1}/><YAxis tickLine={false} axisLine={false} fontSize={9} tickFormatter={axis} width={40}/><ReferenceLine y={0} stroke="#dfe6f0"/><Area type="monotone" dataKey="value" stroke="#2458ff" strokeWidth={2} fill="url(#strategy-report-tod)" dot={false}/><Tooltip/></AreaChart></ResponsiveContainer></div></ReportCard>; }
